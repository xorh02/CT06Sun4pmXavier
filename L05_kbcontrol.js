let shapeColor = "black";
function setup(){
    createCanvas(400,400);
}

function draw(){
    if(key === 'r' || key ==='R'){
        
    }
     if(key === 'e' || key ==='E'){
        
    }
     if(key === 't' || key ==='T'){
        
    }
}